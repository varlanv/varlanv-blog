import { createArticle } from "@/components/feed/Articles";
import { Tag } from "@/types/types";

export default createArticle("Gradle test sync plugin", [Tag.GRADLE], () => {
  return <div></div>;
});
